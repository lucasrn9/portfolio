import { StyledMouse, StyledMouseWrapper, StyledMessage } from "./mouseStyles";

const Mouse = () => (
  <StyledMouseWrapper>
    <StyledMessage>scroll down</StyledMessage>
    <StyledMouse />
  </StyledMouseWrapper>
);

export default Mouse;
